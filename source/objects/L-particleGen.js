var L;
L.objects.PartGen = function() {

};

/*
 * Needs one or more textures
 * Needs source XY
 * Needs source Angle Range
 * Needs source Speed Range
 * Line type = straight, sine, parabola, random
 * Needs switch for whether particles follow source
 * per particle:
 * Needs an xy coord
 * Needs an angle
 * Needs a direction
 * Needs accel/decel
 * Needs decay
 * Needs destroy handler
 */